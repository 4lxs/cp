#include <bits/stdc++.h>
using namespace std;

signed main() {
    int T;
    cin >> T;
    while (T--) {
        int K;
        cin >> K;
        cout << (K % 3 == 1 ? "YES"  : "NO") << endl;
    }
}
